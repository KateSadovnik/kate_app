
<h1><%= current_user&.name %></h1>
<h2><%= current_user&.email %></h2>

<% if @user.items.any? %>
	<h3>Items (<%= @user.items.count %>)</h3>
		<ol class="items">
		<%= render @items %>
		</ol>
	<%= will_paginate @items %>
<% end %>